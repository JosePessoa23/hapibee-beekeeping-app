package com.isep.acme.repositories;

import com.isep.acme.model.Apiario;
import org.springframework.data.repository.CrudRepository;

public interface ApiarioRepository extends CrudRepository<Apiario, Long> {
}
